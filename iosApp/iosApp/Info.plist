<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>

	<!-- Live Activities: the broadcasting / watching session on the Lock Screen and in the
	     Dynamic Island. Without this key ActivityKit refuses every request. -->
	<key>NSSupportsLiveActivities</key>
	<true/>

	<!-- Camera role: capture. Viewer role: scanning a pairing code. -->
	<key>NSCameraUsageDescription</key>
	<string>BabyMonitor Pro uses the camera to stream video to your other devices on this WiFi network, and to scan a camera's pairing code.</string>
	<key>NSMicrophoneUsageDescription</key>
	<string>BabyMonitor Pro uses the microphone to stream sound from this room to your other devices on this WiFi network.</string>

	<!-- Required from iOS 14 for mDNS: without both keys, Bonjour silently finds nothing. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>BabyMonitor Pro finds cameras on your home WiFi. Nothing is sent over the internet.</string>
	<key>NSBonjourServices</key>
	<array>
		<string>_babymonitorpro._tcp</string>
	</array>

	<!-- Plain HTTP to a device on the same WiFi; no traffic leaves the local network. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>

	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.hazemafaneh.babymonitorpro.pairing</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>bmpro</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
